# The project itself, editable, so `cua` is importable from any entry
# point. Without this a fresh clone can run the tests and the service
# (both put src/ on the path themselves) but not discover.py or
# replay.py, which are the two the demo path uses.
-e .

flask==3.0.3
playwright==1.55.0
pydantic==2.9.2
anthropic==1.0.0
python-dotenv==1.0.1
pyyaml==6.0.2
pytest==8.3.3

# Capability API, chatbot and dashboard (adaptation project)
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
jinja2==3.1.6
